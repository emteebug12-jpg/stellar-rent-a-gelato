use soroban_sdk::{testutils::Address as _, Address, Env};

#[test]
#[ignore = "placeholder integration test"]
fn placeholder_test() {
    let env = Env::default();
    let _user = Address::generate(&env);
    assert!(true);
}
